Place .gdbinit in your home directory. Then the command and its documentation will
be available in gdb for use. 

GDB user commands have the following structure:

define command
#do something
end
document command
#write doc string
end

See help inside of gdb for more details.

